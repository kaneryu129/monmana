# domain

学習セッション・しずく・レベル・連続日数のロジックを置く。

**React に依存させないこと。** 純粋な TypeScript として書く。

理由（ADR-0001 備考）: 将来 React Native へ移行する可能性があり、
その際にこの層をそのまま再利用できるようにするため。
UI からの独立は、テストのしやすさにも効く。

`storage/` を直接 import しない。永続化が必要な処理は、
`storage/` が定義するインターフェース越しに受け取る（ADR-0002）。
